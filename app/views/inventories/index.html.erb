<h1>Inventories</h1>
<%= link_to "New inventory", new_inventory_path, class:"btn btn-primary mb-3 mt-3" %>
<div id="inventories">
  <% @inventories.each do |inventory| %>
    <%= render inventory %>
  <% end %>
</div>
