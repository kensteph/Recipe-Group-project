<div class="container">
<%= form_with(model: inventory_food, url:inventory_food_inventory_foods_path(@inventory.id,@food.id)) do |form| %>
  <% if inventory_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_food.errors.count, "error") %> prohibited this inventory_food from being saved:</h2>

      <ul>
        <% inventory_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity ,class:"form-control"%>
  </div>

  <div class="mb-3">
    <%= form.submit %>
  </div>
<% end %>
</div>
